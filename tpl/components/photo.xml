{*  ---------------------------------------------------------
    PHOTO — одиночная фотография.

    Используется как `content`-блок в списках.
    $photo — реальный объект Photo OpenVK.
    --------------------------------------------------------- *}

{block content}
    <div class="photo">
        <img src="{$photo->getURL()}" alt="" />
    </div>
{/block}
