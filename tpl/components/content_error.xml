{*  ---------------------------------------------------------
    CONTENT ERROR — то же, что error.xml, но внутри
    содержимого страницы (используется в закладках, документах).

    Класс content_page_error оставлен: его ожидают некоторые
    страницы OpenVK, которые задают ему высоту.
    --------------------------------------------------------- *}

<div class="message content_page_error">
    <b n:if="!empty($title)">{$title}</b>
    <p>{$description}</p>
</div>
